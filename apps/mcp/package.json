{
  "name": "@akp/mcp",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json && vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@akp/application": "workspace:*",
    "@akp/contracts": "workspace:*",
    "@akp/observability": "workspace:*",
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "tsx": "4.23.1",
    "vitest": "4.1.10"
  }
}
