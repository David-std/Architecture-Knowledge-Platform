# Invariant rule

The same database preserves a transactional invariant.

## Evidence

- deterministic parser keeps source locators
- optional providers require benchmark evidence

| Signal | Value         |
| ------ | ------------- |
| mode   | deterministic |

```sql
SELECT 1;
```

$$R(a,b) = \sum_i w_i$$

![architecture diagram](diagram-placeholder.png)
