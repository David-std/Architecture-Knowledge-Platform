name: P6 Actions probe

on:
  push:
    branches:
      - feat/v0.4-enterprise-context-workspace
  workflow_dispatch:

concurrency:
  group: p6-actions-probe-${{ github.ref }}
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm job graph construction
        run: echo "GitHub Actions created and started a job"
