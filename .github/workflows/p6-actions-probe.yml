name: P6 Actions registered probe

on:
  push:
    branches:
      - feat/v0.4-enterprise-context-workspace
  workflow_dispatch:

concurrency:
  group: p6-actions-registered-probe-${{ github.ref }}
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm job graph construction
        run: echo "registered probe reached a GitHub-hosted runner"
