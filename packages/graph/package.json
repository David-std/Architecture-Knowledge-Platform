{
  "name": "@akp/graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/graph/src/index.js",
  "types": "dist/graph/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@akp/domain": "*"
  },
  "devDependencies": {}
}
