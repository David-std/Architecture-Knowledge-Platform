{
  "name": "@akp/project-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p ../contracts/tsconfig.json && tsc -p tsconfig.build.json && node -e \"const fs=require('node:fs'); for (const f of ['dist/index.js','dist/index.d.ts']) fs.accessSync(f)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@akp/contracts": "*",
    "fast-glob": "^3.3.3"
  },
  "devDependencies": {}
}
